# Production Environment Configuration
# 3-node cluster with full resources

environment: prod

# MWS Single Sign-On - SAML Resolver URL (Prod MWS instance)
mwsSamlResolverUrl: "http://mws-prod.example.com:8585/services/SAML"

# Cluster label for multi-cluster monitoring within same namespace
# Used by Prometheus ServiceMonitors and Grafana dashboards to differentiate clusters
clusterLabel: "prod"

replicaCount: 3

## Image Configuration
## CI/CD pipeline overrides image.tag with immutable SHA tag via:
##   --set image.tag=11.1.0.6-sha-<gitSha>
## The tag below is a fallback for manual deploys only.
image:
  repository: abiwebmethods.azurecr.io/webmethods-microservicesruntime
  tag: "11.1.0.6-build-272"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  ports:
    http: 5555
    https: 5543
    diagnostic: 9999

# Production resources
resources:
  requests:
    memory: "2Gi"
    cpu: "1000m"
  limits:
    memory: "4Gi"
    cpu: "2000m"

# Production JVM memory
jvm:
  minMemory: "1024m"
  maxMemory: "2048m"

# ============================================================================
# Security Context - Production Environment
# ============================================================================
# Uses sagadmin (UID=1724) as standard non-root identity per IBM/SoftwareAG best practices.
# Init container (copy-keyvault-keystores) runs as root with minimal capabilities.
# readOnlyRootFilesystem is NOT enabled - postStart hooks write to container root FS.
#
# Production hardening notes:
# - All Linux capabilities are dropped from MSR container
# - Privilege escalation is disabled
# - Container enforced to run as non-root (sagadmin UID=1724)
# - PVC ownership optimized with fsGroupChangePolicy: OnRootMismatch
# ============================================================================
securityContext:
  pod:
    fsGroup: 1724
    runAsUser: 1724
    runAsGroup: 1724
    runAsNonRoot: true
    fsGroupChangePolicy: "OnRootMismatch"
  container:
    runAsUser: 1724
    runAsGroup: 1724
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

# ============================================================================
# Logging Configuration - Production Environment
# ============================================================================
# For production, use "Warn" level by default for minimal log volume.
# IMPORTANT: Trace level generates significant log volume and impacts performance
#
# To enable DEBUG logging for troubleshooting:
#   helm upgrade wm-msr ./msr-helm -f values-prod.yaml \
#     --set logging.level=Info \
#     --set logging.sap.level=4
# ============================================================================
logging:
  # General IS debug level (Off, Fatal, Error, Warn, Info, Debug, Trace)
  level: "Warn"

  # Enable verbose server logging (helpful for debugging)
  verbose: false

  # Enable service audit logging
  auditLog: false

  # Enable detailed thread logging
  threadDump: false

  # JDBC logging
  jdbc:
    enabled: false
    level: "Warn"

  # SAP Adapter logging configuration
  # Levels: 0=Off, 1=Fatal, 2=Error, 3=Warn, 4=Info, 5=Debug, 6=Trace
  sap:
    # SAP adapter internal log level (0-6, where 6=Trace)
    level: 3  # Default to Warn (3) for production
    # JCo trace level (0-10, where 0=off, 10=most verbose)
    jcoTraceLevel: 0  # Default to off
    # JCo trace output path
    jcoTracePath: "/opt/softwareag/IntegrationServer/instances/default/logs"
    # Facility-specific logging for SAP (format: facility:level)
    facilities: ""

  # Spring Boot / SLF4J logging (for MSR components using Spring)
  spring:
    enabled: false  # Set to true to enable Spring Boot logging config
    rootLevel: "WARN"
    # Component-specific levels (only applied when spring.enabled=true)
    components:
      wm: "WARN"
      softwareag: "WARN"
      sapAdapter: "WARN"
      jco: "WARN"

# Persistence
persistence:
  enabled: true
  storageClass: "managed-premium"
  accessMode: ReadWriteOnce
  size: 10Gi

# ============================================================================
# JDBC POOL Configuration (MSR Internal Database for ISInternal, Xref, etc.)
# ============================================================================
# JDBC Pools are used by MSR internal functions like ISInternal, ISDashboardStats, Xref
# Multiple pools can be configured. Each pool requires:
#   - name: Pool name (used in application.properties)
#   - dbURL: JDBC connection URL (in values file - not sensitive)
#   - username: Database username (in values file - not sensitive)
#   - secretName: Azure Key Vault secret name for password (password only from KV)
#   - passwordEnvVar: Environment variable name for password injection
#
# Pool sizing configuration is environment-specific and non-sensitive
# ============================================================================
jdbcPool:
  enabled: true
  pools:
    # Primary pool for ISInternal, Xref, Dashboard
    - name: "IS-Pool"
      description: "MSR Internal JDBC Pool"
      dbURL: "jdbc:wm:sqlserver://wm-sql-abi-prod.database.windows.net:1433;databaseName=isinternal"
      username: "wmadmin"
      secretName: "jdbcpool-ispool-password"     # Key Vault: prod-jdbcpool-ispool-password
      passwordEnvVar: "JDBC_IS_POOL_PASSWORD"
      driverAlias: "DataDirect Connect JDBC SQL Server Driver"
      dataSourceClass: "com.wm.dd.jdbc.sqlserver.SQLServerDataSource"
      minConns: 5
      maxConns: 300
      poolThreshold: 10
      waitingThread: 100
      expireTime: 60000
      useSSL: false

    # Example: Additional pool for custom application database
    # - name: "App-Pool"
    #   description: "Custom Application JDBC Pool"
    #   dbURL: "jdbc:wm:sqlserver://app-sql-prod.database.windows.net:1433;databaseName=APP_PROD"
    #   username: "app_user"
    #   secretName: "jdbcpool-apppool-password"  # Key Vault: prod-jdbcpool-apppool-password
    #   passwordEnvVar: "JDBC_APP_POOL_PASSWORD"
    #   driverAlias: "DataDirect Connect JDBC SQL Server Driver"
    #   dataSourceClass: "com.wm.dd.jdbc.sqlserver.SQLServerDataSource"
    #   minConns: 5
    #   maxConns: 100
    #   poolThreshold: 10
    #   waitingThread: 50
    #   expireTime: 60000
    #   useSSL: true

  # JDBC Functional Aliases map logical names to physical pool names
  functionalAliases:
    - name: "ISInternal"
      connPoolAlias: "IS-Pool"
      failFastMode: true
    - name: "ISDashboardStats"
      connPoolAlias: "IS-Pool"
      failFastMode: false
    - name: "Xref"
      connPoolAlias: "IS-Pool"
      failFastMode: true

# ============================================================================
# JDBC ADAPTER Configuration
# ============================================================================
# JDBC Adapter connections are configured in a separate file for maintainability.
# See: adapters/values-jdbc-adapter-prod.yaml
#
# Usage:
#   helm upgrade --install wm-msr ./msr-helm \
#     -f values-prod.yaml \
#     -f adapters/values-jdbc-adapter-prod.yaml \
#     -n webmethods
# ============================================================================

# ============================================================================
# SAP ADAPTER Configuration
# ============================================================================
# SAP Adapter connections and listeners are configured in a separate file.
# See: adapters/values-sap-adapter-prod.yaml
#
# Usage:
#   helm upgrade --install wm-msr ./msr-helm \
#     -f values-prod.yaml \
#     -f adapters/values-sap-adapter-prod.yaml \
#     -n webmethods
# ============================================================================

# High availability with pod anti-affinity (required for production)
affinity:
  podAntiAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      - topologyKey: kubernetes.io/hostname
        labelSelector:
          matchLabels:
            app: wm-msr

# Pod Disruption Budget (ensure at least 2 pods available)
podDisruptionBudget:
  enabled: true
  minAvailable: 2

# Health Probes (conservative for prod)
healthProbes:
  startup:
    enabled: true
    path: /
    port: 5555
    initialDelaySeconds: 90
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 40
  liveness:
    enabled: true
    path: /
    port: 5555
    initialDelaySeconds: 180
    periodSeconds: 30
    timeoutSeconds: 10
    failureThreshold: 3
  readiness:
    enabled: true
    path: /
    port: 5555
    initialDelaySeconds: 90
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3

# ============================================================================
# License Configuration (files/prod/license/)
# ============================================================================
# Set enabled: true ONLY after placing your real license files.
# Mounting placeholder files will cause startup errors.
#
# Steps to enable:
#   1. Replace files/prod/license/licenseKey.xml with your actual IS license
#   2. Replace files/prod/license/terracotta-license.key with your actual TC license
#   3. Set license.enabled: true below
#   4. Redeploy: helm upgrade wm-msr ./msr-helm -f values-prod.yaml
# ============================================================================
license:
  enabled: false

# ============================================================================
# Extended Settings (watt.* Properties via Config Template)
# ============================================================================
# Additional watt.* properties added to application.properties at startup.
# Uses settings.watt.* prefix per IBM Configuration Variables Template format.
# Supports both inline properties map AND file-based extended-settings.cnf
#
# In the properties map below, use bare watt.* keys — chart auto-adds "settings." prefix.
# In the .cnf file, use full settings.watt.* prefix.
# ============================================================================
extendedSettings:
  enabled: true

  properties:
    # URL Aliases (environment-specific)
    # watt.server.url.alias.myapi: "/rest/myapi"
    # watt.server.url.alias.health: "/health"
    # Thread pool and connection tuning (prod values)
    # watt.net.maxClientKeepaliveConns: "100"
    # watt.server.threadPool: "500"

# ============================================================================
# Custom Wrapper Configuration (custom_wrapper.conf)
# ============================================================================
# JVM wrapper configuration loaded from files/prod/configuration/custom_wrapper.conf
# Controls JVM options, library paths, wrapper timeouts, etc.
# Read at JVM startup BEFORE Integration Server starts
# ============================================================================
customWrapper:
  enabled: true

# ============================================================================
# Web Services Endpoint Configuration
# ============================================================================
# Endpoint .cnf files auto-discovered from files/prod/config/endpoints/*.cnf
# Mounted to /opt/softwareag/.../config/endpoints/ via PostStart copy
# ============================================================================
endpointConfigs:
  enabled: true

# JMS/JNDI Configuration (UM JMS Provider)
jmsConfig:
  enabled: true

# Universal Messaging Configuration
# Passwords stored in Azure Key Vault: prod-um-biz-password, prod-um-logs-password
um:
  enabled: true
  connections:
    # Business UM Connection (Active-Passive Cluster)
    - connectionAlias: "IS_UM_BUSINESS"
      connectionEnabled: true
      url: "nsp://um-biz:9000"
      user: "Administrator"
      useCSQ: "true"
      csqSize: "-1"
      csqDrainInOrder: "true"
      secretName: "um-business-password"
      passwordEnvVar: "UM_BUSINESS_PASSWORD"
    # Logs UM Connection (Active-Active Cluster - Combined Service)
    - connectionAlias: "IS_UM_LOGS"
      connectionEnabled: true
      url: "nsp://um-logs-combined:9000"
      user: "Administrator"
      useCSQ: "true"
      csqSize: "-1"
      csqDrainInOrder: "true"
      secretName: "um-logs-password"
      passwordEnvVar: "UM_LOGS_PASSWORD"

# =====================
# CACHING CONFIGURATION
# =====================
# Public Cache Managers loaded from files/config/caching/*.xml
# Each XML defines an Ehcache cache manager with named caches
# Included: OrderCache (ActiveOrders, OrderHistory),
#           SessionCache (UserSessions, AuthTokens),
#           LookupCache (CountryCodes, CurrencyRates, ProductCatalog)
caching:
  publicCacheManagers:
    enabled: true

# Terracotta Cache Manager Configuration
terracotta:
  enabled: true
  cacheManagerName: "IS_TERRACOTTA_CACHE"
  urls:
    - "terracotta-0.terracotta-service:9510"
    - "terracotta-1.terracotta-service:9510"
  # Only 2 Terracotta servers are deployed, matching StatefulSet replicas

# Keystore Configuration (example - for production use Azure Key Vault)
# To use actual keystore files, add 'fileData' field with base64 encoded content
keystores: []
  # - aliasName: "DEFAULT_IS_KEYSTORE"
  #   description: "Default Integration Server Keystore for SSL/TLS"
  #   type: "PKCS12"
  #   provider: "SUN"
  #   location: "/opt/softwareag/IntegrationServer/config/security/keystore.p12"
  #   password: "manage"
  #   isHsm: false
  #   keys:
  #     - keyAliasName: "sslkey"
  #       keyAliasPassword: "manage"
  #   # fileData: ""  # Base64 encoded keystore file

# Truststore Configuration (example - for production use Azure Key Vault)
truststores: []
  # - aliasName: "DEFAULT_IS_TRUSTSTORE"
  #   description: "Default Integration Server Truststore for SSL/TLS"
  #   type: "JKS"
  #   provider: "SUN"
  #   location: "/opt/softwareag/IntegrationServer/config/security/truststore.jks"
  #   password: "manage"
  #   # fileData: ""  # Base64 encoded truststore file

# Autoscaling Configuration (Production)
autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  # Memory metric disabled for JVM apps - JVM heap is constant regardless of load,
  # causing HPA to never scale down. Scale on CPU only.
  targetMemoryUtilizationPercentage: false

# ============================================================================
# Azure Key Vault Integration
# ============================================================================
# Store certificates in Azure Key Vault for centralized management
# Two types of certificates can be stored:
#
# 1. KEYSTORE Certificate (Certificate + Private Key)
#    - Used for YOUR server's identity (SSL/TLS server certificate)
#    - Must be imported as PKCS12/PFX with private key
#    - CSI driver mounts both .crt and .key files
#    - Init container converts PEM to PKCS12 keystore
#
# 2. TRUSTSTORE Certificate (Certificate only, NO Private Key)
#    - Used for TRUSTED external parties (CA certs, partner certs)
#    - Imported as .crt, .cer, or .pem (no private key)
#    - CSI driver mounts only .crt file
#    - Init container combines multiple certs into PKCS12 truststore
#
# IMPORTANT: Azure Key Vault Access Requirements
# -----------------------------------------------
# The AKS kubelet managed identity must have access to the Key Vault.
# Production Best Practices:
# - Use separate Key Vault for production (wM-kv-prod)
# - Enable soft-delete and purge protection on Key Vault
# - Use Premium SKU Key Vault for production workloads
# - Set up certificate expiration alerts
# ============================================================================
azureKeyVault:
  enabled: true
  vaultName: "wM-kv-prod"
  tenantId: "6824e2ad-c1e1-4119-912d-c1f42857e416"
  clientId: ""  # CSI Secrets Store addon identity - populate when prod cluster is provisioned
  mountPath: "/mnt/secrets-store"

  # Environment-specific prefix for secret names in Azure Key Vault
  # Secrets will be retrieved as: {secretKeyPrefix}-{secretName}
  # Example: prod-jdbc-pool-password, prod-keystore-password, prod-um-password
  secretKeyPrefix: "prod"

  # KEYSTORE Certificates (Certificate + Private Key)
  # These certificates represent YOUR server's identity
  # Upload to Azure Key Vault as Certificate with private key (PKCS12/PFX)
  # Passwords stored in Azure Key Vault: prod-keystore-password, prod-keyalias-password
  certificates:
    - certName: "wm-cer"
      fileName: "wm_keystore.p12"
      keystoreConfig:
        aliasName: "WM_KEYSTORE"
        description: "webMethods Keystore from Azure Key Vault (Prod)"
        type: "PKCS12"
        provider: "SUN"
        # Password injected via environment variable from Azure Key Vault
        isHsm: false
        keys:
          - keyAliasName: "wm-cer"
            # Key alias password injected via environment variable from Azure Key Vault

  # Additional secrets can be stored here (not commonly used)
  secrets: []

# ============================================================================
# TRUSTSTORE Configuration
# ============================================================================
# Truststore contains trusted certificates (CA certs, partner certs)
# Each certificate should be uploaded to Azure Key Vault as Certificate object
# WITHOUT private key (just the .crt file)
#
# How it works:
# 1. Upload trusted CA certificates to Azure Key Vault (Certificate section)
# 2. List them below in the certificates array
# 3. Init container fetches all certificates from Key Vault
# 4. Init container combines them into single PKCS12 truststore with --legacy flag
# 5. MSR loads the truststore
#
# Example: Upload a trusted CA certificate
#   az keyvault certificate import \
#     --vault-name wM-kv-prod \
#     --name truststore-root-ca \
#     --file root-ca.crt
# ============================================================================
truststoreCertificates:
  enabled: true
  fileName: "wm_truststore.p12"
  aliasName: "WM_TRUSTSTORE"
  description: "webMethods Truststore from Azure Key Vault (Prod)"
  type: "PKCS12"
  provider: "SUN"
  # Password stored in Azure Key Vault: prod-truststore-password
  # Password injected via environment variable from Azure Key Vault

  # List of trusted certificates to include in truststore
  # Each certificate must exist in Azure Key Vault (Secrets section as PEM files)
  # These certificates are public certificates only (no private keys)
  # Certificate names in Key Vault are prefixed with secretKeyPrefix: prod-truststore-root-ca, etc.
  certificates:
    - certName: "truststore-root-ca"          # Root CA certificate (Key Vault: prod-truststore-root-ca)
      alias: "root-ca"                        # Alias in PKCS12 truststore
    - certName: "truststore-intermediate-ca"  # Intermediate CA certificate (Key Vault: prod-truststore-intermediate-ca)
      alias: "intermediate-ca"
    - certName: "truststore-partner-cert"     # Partner's public certificate (Key Vault: prod-truststore-partner-cert)
      alias: "partner-cert"
    - certName: "truststore-payment-gateway"  # Payment Gateway partner certificate (Key Vault: prod-truststore-payment-gateway)
      alias: "payment-gateway"
    - certName: "truststore-banking-partner"  # Banking partner certificate (Key Vault: prod-truststore-banking-partner)
      alias: "banking-partner"

# ============================================================================
# PACKAGE CONFIGURATIONS (File-based)
# ============================================================================
# Package config files are auto-discovered from files/prod/packages/*/config/*
# Current packages: AbTest, AbTest2
# To add a new package config, create: files/prod/packages/{PkgName}/config/app.properties
# Sensitive values use ${ENV_VAR} syntax, with secrets in Azure Key Vault
# ============================================================================
packageConfigs:
  enabled: true

# ============================================================================
# ESB APP CONFIGS (Environment-Specific)
# ============================================================================
# ESB App config files auto-discovered recursively from files/prod/ESB_App_Configs/
# Current packages: SmOMS, SmPublic, SmSES_v33 (with deep subdirectory structure)
# Copied to /opt/softwareag/ESB_App_Configs/ preserving full directory tree
# ============================================================================
esbAppConfigs:
  enabled: true

# ============================================================================
# GLOBAL VARIABLES (globalVariables.cnf)
# ============================================================================
# Loaded from files/prod/config/globalVariables.cnf
# Mounted to /opt/softwareag/IntegrationServer/config/globalVariables.cnf
# ============================================================================
globalVariables:
  enabled: true

# ============================================================================
# FILE ACCESS CONTROL Configuration (WmPublic package - pub.file services)
# ============================================================================
# Controls which directories/files the pub.file services can read, write, delete
# This creates fileAccessControl.cnf in WmPublic/config directory
#
# Path Format:
# - Use semicolons (;) to separate multiple paths (no spaces)
# - Single asterisk (*) matches directory names
# - Double asterisk (**) matches multiple nested folders and files
# - Paths are case-sensitive on Linux
#
# Reference: https://www.ibm.com/docs/en/webmethods-integration/wm-integration-server/10.15.0?topic=guide-file-folder
# ============================================================================
fileAccessControl:
  enabled: true

  # Directories/files with READ permission for pub.file services
  # Prod environment: Restrictive - only necessary paths
  allowedReadPaths: "/tmp;/opt/softwareag/IntegrationServer/instances/default/logs/**;/opt/softwareag/IntegrationServer/instances/default/config/**;/opt/softwareag/IntegrationServer/instances/default/data/**;/dev/smud456/**;/dev/smud123/**"

  # Directories/files with WRITE permission for pub.file services
  # Prod environment: Restrictive - only necessary paths
  allowedWritePaths: "/tmp;/opt/softwareag/IntegrationServer/instances/default/logs/**;/opt/softwareag/IntegrationServer/instances/default/data/**;/dev/smud456/**;/dev/smud123/**"

  # Directories/files with DELETE permission for pub.file services
  # Prod environment: Most restrictive - temp only
  allowedDeletePaths: "/tmp"

# ============================================================================
# webMethods Cloud (Integration Cloud) Configuration
# ============================================================================
# Enables connectivity to webMethods.io Integration / Integration Cloud
#
# Configuration files are loaded from:
#   - files/integrationlive/accounts.cnf
#   - files/integrationlive/connections.cnf
#   - files/integrationlive/applications/*.cnf
#
# Password stored in Azure Key Vault: prod-wmcloud-prod-io-password
# ============================================================================
webMethodsCloud:
  enabled: false

  # Cloud Passwords (Azure Key Vault)
  # Only enable passwords for accounts that are enabled in accounts.cnf
  cloudPasswords:
    - alias: "Prod io"
      secretName: "wmcloud-prod-io-password"
      envVar: "WMCLOUD_PROD_IO_PASSWORD"

  # Optional: Override application files from values (normally loaded from files/)
  applicationFiles: {}
